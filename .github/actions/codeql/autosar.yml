name: "CodeQL AUTOSAR Coding Standard"

disable-default-queries: true

queries:
  packs:
  - github/codeql-coding-standards/cpp/autosar/src/codeql-suites/autosar-default.qls@main

query-filters:
  - exclude:
      problem.severity:
        - recommendation
